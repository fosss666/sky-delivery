<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealMapper">


    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into sky_take_out.setmeal(category_id, name, price, description, image, create_time, update_time,
                                         create_user, update_user)
        VALUES (#{setmeal.categoryId}, #{setmeal.name}, #{setmeal.price}, #{setmeal.description}, #{setmeal.image},
                #{setmeal.createTime}, #{setmeal.updateTime}, #{setmeal.createUser}, #{setmeal.updateUser})
    </insert>
    <select id="selectList" resultType="com.sky.vo.SetmealVO">
        select s.*, c.name as categoryName
        from sky_take_out.setmeal s
        left join sky_take_out.category c on s.category_id = c.id
        <where>
            <if test="categoryId !=null">
                s.category_id=#{categoryId}
            </if>
            <if test="name != null">
                and s.name like concat('%',#{name},'%')
            </if>
            <if test="status != null">
                and s.status=#{status}
            </if>
        </where>
        order by update_time desc
    </select>
</mapper>
